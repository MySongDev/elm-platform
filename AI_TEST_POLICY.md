# AI 测试准入政策

本文件是 Elm Platform 的测试决策唯一权威来源，适用于 Claude Code、Codex、Copilot、GPT、Cursor 及其他在本仓库工作的 AI 工具。

## 默认规则

默认**不新增、不修改、不删除**自动化测试，包括单元测试、集成测试、端到端测试和快照测试。不要为了覆盖率、形式完整或惯例而补测。

只有本政策“允许新增测试”的场景成立，或用户明确要求时，才可调整测试。

## 允许新增或修改测试的场景

以下任一条件满足时，测试通常是必要的：

1. 独立工具函数：数据转换、格式化、解析、校验、金额计算等可脱离 UI 和外部服务验证的逻辑；
2. 复杂业务模块：多条件分支、状态机、权限判定、重试/幂等、库存或订单状态流转等容易回归的逻辑；
3. 核心数据流转 API：认证授权、下单、库存扣减、订单创建与状态变更、支付回调等；
4. 金融或资金相关逻辑：支付、退款、结算、对账、提现、分账、金额精度、重复提交防护；
5. 修复明确的线上或历史缺陷，且测试能稳定复现缺陷并防止回归；
6. 用户明确要求新增、修改或补齐测试。

即使满足条件，也只测试本次变更的关键行为、失败路径、边界条件与金额/状态不变量；复用既有测试结构，不引入大规模测试脚手架。

## 通常不写测试的场景

以下改动默认不写测试：

- UI 样式、布局、文案、图标、静态展示或交互微调；
- 简单 Vue/React 组件展示层改动；
- 路由、菜单、配置、常量、类型声明的常规调整；
- 简单 CRUD 页面、表单字段和列表展示调整；
- 已有类型检查、lint、构建或最小人工验证足以覆盖的改动；
- 对实现细节、样式细节或第三方库行为的测试。

## 不确定时的处理

当测试必要性不明确时：

1. 不要自行创建或改动测试；
2. 在最终说明中标记“未新增测试”，并说明依据；
3. 选择与改动匹配的最小验证：type-check、lint、build 或手动 smoke check；
4. 只有用户要求判断或确认后，才扩大到测试实现。

## AI 执行要求

- 实施前先按本政策判断测试是否必要；
- 需要测试时，先在执行说明或最终总结中交代触发的准入条件；
- 不得因本政策而跳过已有测试的修复：若现有测试因本次改动失败，应修复、更新或明确报告原因；
- 本政策约束“新增/变更测试”的决策，不替代项目既有的构建、lint、类型检查和人工验收要求。
